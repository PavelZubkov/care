namespace $.$$ {

	const { per } = $mol_style_unit

	$mol_style_define( $.$care_app_request_add , {

		Body: {
			padding: $mol_gap.block,
		},

		$mol_textarea: {
			maxWidth: per(100),
		},

	} )

}
